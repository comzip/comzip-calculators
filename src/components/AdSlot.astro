---
// Placeholder for a future ad unit (AdSense, etc.).
// Positioned where an ad would appear on calculator pages, e.g. below the
// result and above the FAQ section. No real ad code is included yet.
//
// Renders nothing for now. An earlier version rendered a visible "광고 영역
// (준비 중)" placeholder box, but that text is present in every calculator
// page's crawled content (38 pages) and reads as an unfinished-page signal
// during AdSense review. Once AdSense is approved, replace the empty
// template below with the real <ins class="adsbygoogle"> unit markup.
import type { Lang } from '../i18n/ui';

export interface Props {
  lang?: Lang;
}
---
