---
// Placeholder for a future ad unit (AdSense, etc.).
// Positioned where an ad would appear on calculator pages, e.g. below the
// result and above the FAQ section. No real ad code is included yet.
---

<div class="ad-slot" aria-hidden="true">
  <!-- TODO: AdSense slot -->
  <!-- Replace this placeholder with the real ad unit markup once AdSense is approved. -->
  <span>광고 영역 (준비 중)</span>
</div>

<style>
  .ad-slot {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 90px;
    margin: 24px 0;
    border: 1px dashed var(--border);
    border-radius: var(--radius);
    color: var(--text-muted);
    font-size: 0.8rem;
    background: var(--bg-soft);
  }
</style>
